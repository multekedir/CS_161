#include <iostream>

#include <string>


using namespace std;

int  a_to_i(char letter){

    return letter;
}

char i_to_a(char num){
    if(num >= '0' && num <= '9')
        return (int) num;
    return '0';

}

int main(){
    char  m;
    cin >> m;
      cout <<  i_to_a(m) << endl;
}
